# Построение выпуклой оболочки — проход Грэхема (ALL)

- Студент: Дергачёв Арсений Сергеевич
- Группа: 3823Б1ПР3
- Технология: ALL (MPI + OpenMP)
- Вариант: 22

## 1. Контекст

Гибридная реализация проверяет способность алгоритма масштабироваться за пределы одного узла с использованием MPI и
OpenMP. Задача выпуклой оболочки является сложной для распределенных систем из-за глобальной зависимости точек.

## 2. Постановка задачи

Задана генерация `n = 500 000` точек, среди которых необходимо построить выпуклую оболочку методом Грэхема. Базовым
эталоном производительности служит версия `SEQ`.

## 3. Базовый алгоритм

Базовый алгоритм Грэхема: поиск опорной точки `O(n)`, сортировка `O(n log n)`, фильтрация стеком `O(n)`. В
распределенной модели данные делятся на непересекающиеся множества, локально вычисляются выпуклые оболочки, а затем эти
фрагменты собираются в единую оболочку.

## 4. Межпроцессная схема

- **Распределение:** Нулевой ранг генерирует полный набор точек и использует `MPI_Bcast` для рассылки числа
  вершин `n`. Далее `MPI_Scatterv` распределяет точки по всем процессам. Данные сериализуются в одномерный
  массив для пересылки.
- **Сборка:** Локальные оболочки, рассчитанные каждым процессом, сериализуются обратно. Их размеры
  отправляются через `MPI_Gather`, а сами точки собираются на rank 0 с помощью `MPI_Gatherv`.
- **Широковещание результата:** Нулевой процесс строит итоговую оболочку и рассылает ее всем остальным процессам
  через `MPI_Bcast`, чтобы каждый процесс имел консистентное состояние и вернул идентичный ответ в PostProcessingImpl.

## 5. Внутрипроцессная схема

Внутри каждого MPI-процесса применяется OpenMP. Локальная оболочка строится функцией `BuildHull`, которая выполняет
полный проход Грэхема над своим фрагментом данных:

- параллельный поиск опорной точки (`FindPivotIndex` с `#pragma omp parallel for`);
- последовательная сортировка по полярному углу;
- последовательное построение стека оболочки.

Генерация точек в `PreProcessingImpl` также распараллелена через `#pragma omp parallel for`. Число потоков задаётся
переменной окружения `PPC_NUM_THREADS` через `ppc::util::GetNumThreads()`.

## 6. Детали реализации

Файлы: `all/include/ops_all.hpp`, `all/src/ops_all.cpp`

Предыдущая версия (MPI + `std::thread`) делила локальные точки на чанки и строила частичные оболочки в отдельных
потоках, что давало многократный пересчёт и высокие накладные расходы. Новая версия заменяет это на один корректный
проход Грэхема с OpenMP-ускорением только на параллелизуемых фазах.

Основной цикл межпроцессного обмена:

```cpp
MPI_Scatterv(flat_all.data(), send_counts.data(), send_displs.data(), MPI_DOUBLE,
             local_flat.data(), local_size, MPI_DOUBLE, 0, MPI_COMM_WORLD);
std::vector<Pt> local_hull = BuildHull(std::move(local_pts));
MPI_Gatherv(hull_flat.data(), local_hull_flat_size, MPI_DOUBLE, gathered_flat.data(),
            recv_counts.data(), recv_displs.data(), MPI_DOUBLE, 0, MPI_COMM_WORLD);
```

## 7. Проверка корректности

Сравнение с SEQ: логика проверена на малых и средних размерах. Сборка оболочек из частичных оболочек является корректным
математическим приемом: любая точка, не входящая в локальную выпуклую оболочку, гарантированно не войдет в глобальную.
Сборка глобальной оболочки над всеми вершинами локальных оболочек дает точный ответ.

## 8. Экспериментальная среда

- **CPU:** AMD Ryzen 7 9800X3D @ 4.7–5.2 GHz (8 ядер / 16 потоков)
- **RAM:** 32 ГБ DDR5
- **OS:** Windows 11 Pro 10.0.26200
- **Компилятор:** MSVC 19.50 (Release сборка)
- **Размер задачи:** `n = 500 000` точек
- **Команды запуска:**
  - Обычный запуск: `.\ppc_perf_tests --gtest_filter="*dergachev_a_graham_scan*"`
  - Запуск с потоками: `$env:PPC_NUM_THREADS="<N>"; .\ppc_perf_tests --gtest_filter="*dergachev_a_graham_scan*"`
  - Запуск с MPI: `$env:PPC_NUM_THREADS="<N>"; mpiexec -n <N> .\ppc_perf_tests --gtest_filter="*dergachev_a_graham_scan*"`

## 9. Результаты

Базовое время `SEQ` (task_run): **0.0170 с**. Предыдущая реализация MPI + STL на 1 процессе давала **0.2237 с**
(замедление в ~13 раз относительно SEQ).

### Таблица 1. Режим task_run (MPI + OpenMP)

| Процессы | Потоки/Процесс | Время, с | Ускорение (отн. SEQ) | Эффективность (по процессам) |
| --- | --- | --- | --- | --- |
| 1 | 1 | 0.0380 | 0.45 | 44.7% |
| 2 | 2 | 0.0342 | 0.50 | 24.8% |
| 4 | 4 | 0.0360 | 0.47 | 11.8% |
| 8 | 8 | 0.0982 | 0.17 | 2.2% |

### Таблица 2. Режим task_pipeline (MPI + OpenMP)

| Процессы | Потоки/Процесс | Время, с | Ускорение (отн. SEQ) | Эффективность (по процессам) |
| --- | --- | --- | --- | --- |
| 1 | 1 | 0.0439 | 0.39 | 38.7% |
| 2 | 2 | 0.0361 | 0.47 | 23.6% |
| 4 | 4 | 0.0388 | 0.44 | 10.9% |
| 8 | 8 | 0.0790 | 0.22 | 2.7% |

### Сравнение с предыдущей реализацией (MPI + STL)

| Конфигурация | task_run (STL) | task_run (OpenMP) | Улучшение |
| --- | --- | --- | --- |
| 1×1 | 0.2237 | 0.0380 | **5.9×** |
| 4×4 | 0.1490 | 0.0360 | **4.1×** |
| 8×8 | 0.1467 | 0.0982 | **1.5×** |

На 1–4 процессах время снизилось в 4–6 раз. Конфигурация 8×8 по-прежнему страдает от переподписки (64 потока на 16
логических ядер), но всё равно быстрее прежней STL-версии.

## 10. Выводы

Переход с MPI + `std::thread` на MPI + OpenMP устранил главную ошибку проектирования — построение оболочки по
произвольным чанкам вместо полного алгоритма Грэхема. Накладные расходы MPI по-прежнему заметны на малом числе
процессов, однако итоговое время стало сопоставимым с чисто OpenMP-реализацией и значительно лучше прежней гибридной
схемы.
